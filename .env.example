# Turso (required for RSVP storage)
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Optional: if set, GET /api/rsvp requires ?token=<value>
# Leave empty to allow public guest-list reads.
RSVP_ADMIN_TOKEN=
